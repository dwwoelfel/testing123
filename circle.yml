machine:
  pre:
    - echo "hi daniel"
  services:
    - redis-server

database:
  override:
    - node --version

test:
  override:
    - sh parallel.sh

deployment:
  master:
    branch: master
    commands:
      - exit 1
      - echo 'hi'
