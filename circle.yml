machine:
  services:
    - elasticsearch

database:
  override:
    - echo 'nope'

test:
  pre:
    - "[ $CIRCLE_NODE_INDEX == 0 ] && bundle exec rake spec:core"
    - "[ $CIRCLE_NODE_INDEX == 1 ] && bundle exec rake spec:integration"
