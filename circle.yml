machine:
  ruby:
    version: 2.0.0-p195

# database:
#   override:
#     - echo skip db

dependencies:
  cache_directories:
    - "~/bin"

# deployment:
#   stage:
#     branch: /.+/
#     commands:
#       - git push git@heroku.com:circleci.git $CIRCLE_SHA1:refs/heads/master

# notify:
#   webhooks:
#     - url: http://requestb.in/1k5dzpe1

notify:
  webhooks:
    - url: http://requestb.in/p7t2k3p7
