machine:
  timezone: America/Los_Angeles
  ruby:
    version: 2.0.0-p0
  hosts:
    "platform.dev clinic.platform.dev api.platform.dev developer.platform.dev admin.platform.dev assets0.platform.dev assets1.platform.dev assets2.platform.dev": 127.0.0.1




database:
  override:
    - echo skip db

# deployment:
#   stage:
#     branch: /.+/
#     commands:
#       - git push git@heroku.com:circleci.git $CIRCLE_SHA1:refs/heads/master

# notify:
#   webhooks:
#     - url: http://requestb.in/1k5dzpe1
