machine:
  node:
    version: 0.10.0

database:
  override:
    - echo skip db


test:
  pre:
    - echo $CIRCLE_NODE_INDEX
  override:
    - echo $CIRCLE_NODE_INDEX
  post:
    - echo $CIRCLE_NODE_INDEX
