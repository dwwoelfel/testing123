machine:
  pre:
    - echo "hi daniel"
  services:
    - redis-server

dependencies:
  pre:
    - ruby -e '`cat /dev/zero`': {timeout: 1000}

database:
  override:
    - node --version

test:
  override:
    - sh parallel.sh

deployment:
  master:
    branch: master
    commands:
      - exit 1
      - echo 'hi'
