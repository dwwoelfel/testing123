dependencies:
  pre:
    - nvm install 0.8.19
    - nvm alias default 0.8.19
    - curl -sS https://getcomposer.org/installer | php

database:
  override:
    - echo 'nope'

deployment:
  master:
    branch: master
    commands:
      - exit 1
