database:
  override:
    - node --version

dependencies:
  pre:
    - echo "this is a pre from yml"
  override:
    - echo "this is an overrride from yml"
  post:
    - echo "this is a post from yml"

deployment:
  master:
    branch: master
    commands:
      - exit 1
      - echo 'hi'
