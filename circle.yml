machine:
  services:
    - elasticsearch
    - solr
    - postgresql

database:
  pre:
    - createdb template_postgis
    - psql -U postgres -d template_postgis -c "create extension postgis"
    - createdb circle_ruby_test -T template_postgis
