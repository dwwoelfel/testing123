machine:
  pre:
    - echo "hi daniel"
  services:
    - redis-server
  ruby:
    version: 1.9.3-p374

database:
  override:
    - node --version

test:
  override:
    - sh parallel.sh

deployment:
  master:
    branch: master
    commands:
      - exit 1
      - echo 'hi'
